{
  "name": "resume-ai-screener-electron",
  "version": "1.0.33",
  "description": "本地简历岗位匹配评分系统：修复API Key保存时报setStoredApiKey未定义的问题，并补强自检。",
  "main": "main.js",
  "author": "Resume AI Screener",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "electron .",
    "dist": "electron-builder",
    "dist:win": "electron-builder --win nsis portable --publish never",
    "dist:dir": "electron-builder --dir",
    "selfcheck": "node scripts/selfcheck.js",
    "check": "node --check main.js && node --check src/main/main.js && node --check src/main/preload.js && node --check src/renderer/renderer.js && node scripts/selfcheck.js"
  },
  "dependencies": {
    "mammoth": "^1.8.0",
    "pdf-parse": "^1.1.1",
    "tesseract.js": "^6.0.0"
  },
  "devDependencies": {
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8"
  },
  "build": {
    "appId": "com.resume.ai.screener",
    "productName": "简历岗位匹配评分系统",
    "directories": {
      "output": "dist"
    },
    "files": [
      "main.js",
      "package.json",
      "src/**/*",
      "!**/*.map",
      "!**/.DS_Store"
    ],
    "win": {
      "icon": "src/assets/app-icon.ico",
      "target": [
        "nsis",
        "portable"
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "简历岗位匹配评分系统",
      "installerIcon": "src/assets/app-icon.ico",
      "uninstallerIcon": "src/assets/app-icon.ico"
    },
    "asar": false
  }
}