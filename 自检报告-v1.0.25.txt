简历岗位匹配评分系统 v1.0.25 自检报告

一、本次新增功能
1. 0.接口密钥设置区可折叠/展开。
2. 1.岗位标准设置区可折叠/展开。
3. 左侧功能区可折叠/展开。
4. 支持浏览器式页面缩放：Ctrl + 鼠标滚轮、Ctrl + +、Ctrl + -、Ctrl + 0。
5. 缩放比例会保存到本地 settings.json。
6. 界面调整为更接近 ChatGPT 网页版的结构：左侧为功能区，右侧为操作区。
7. 优化页面整体横向溢出，表格在内部滚动，页面主体尽量不需要左右移动。

二、回归自检结果
- JS语法：main.js：通过；
- JS语法：src/main/main.js：通过；
- JS语法：src/main/preload.js：通过；
- JS语法：src/renderer/renderer.js：通过；
- HTML ID 与前端调用：通过；[]
- 按钮绑定：通过；[]
- IPC接口对应：通过；[]
- 历史误替换残留：通过；{'path.常驻name': 0, 'get深度求索Models': 0, 'export排行榜表格': 0, 'total令牌s': 0, 'prompt令牌s': 0, '兼容兼容': 0, 'undefined is not': 0, 'function undefined': 0, 'async async function': 0}
- 0接口密钥设置区折叠：通过；可展开/收起
- 1岗位标准设置区折叠：通过；可展开/收起
- 左侧功能区折叠：通过；可展开/收起
- 浏览器式Ctrl缩放：通过；支持 Ctrl+鼠标滚轮 / Ctrl++ / Ctrl+- / Ctrl+0
- 缩放比例持久化：通过；写入 settings.json
- ChatGPT式左右布局：通过；左侧功能区，右侧操作区
- 减少横向溢出：通过；页面主体不整体横向滚动，表格内部横向滚动
- PDF/Word/TXT读取：通过；通过
- 模型选择非空兜底：通过；通过
- resetLeaderboardFilters：通过；通过
- 危险操作二次确认：通过；通过
- 默认岗位无明确指向：通过；通过
- 证据置信度校准：通过；通过
- 严格度隔离排行榜：通过；通过
- 排行榜分页：通过；通过
- 排行榜不显示单人Token：通过；通过
- 检查结果折叠：通过；通过
- Token合并：通过；通过
- 双通道OCR预提取：通过；通过
- 图片最多9张限制：通过；通过
- DeepSeek不直接接收图片：通过；通过
- tesseract依赖：通过；通过
- GitHub Actions：通过；通过
- 版本号：通过；通过

三、建议人工测试
1. 打开软件后确认首页显示 v1.0.25。
2. 点击“0.接口密钥”的收起/展开按钮。
3. 点击“1.岗位标准设置”的收起/展开按钮。
4. 点击左侧顶部 ☰，确认左侧功能区可折叠和展开。
5. 按 Ctrl + 鼠标滚轮、Ctrl + +、Ctrl + -、Ctrl + 0，确认页面缩放正常。
6. 最大化窗口后确认页面主体不需要整体左右移动；如排行榜列多，仅表格内部横向滚动。
7. 上传 PDF / Word / TXT / 图片截图测试历史功能是否正常。