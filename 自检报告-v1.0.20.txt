简历岗位匹配评分系统 v1.0.20 自检报告

一、本次修复的问题
1. 修复模型选择下拉框为空的问题。
2. 修复 resetLeaderboardFilters 函数缺失导致初始化可能中断的问题。
3. 调整模型列表兜底策略，HTML 和 JS 都保留默认模型选项。
4. 默认模型调整为兼容推理模型。

二、本次新增的排行榜规则
1. 排行榜新增“严格度排行”筛选。
2. 1度、2度、3度、4度、5度评分互不混排。
3. 用户必须选择对应严格度，才能看到该严格度下的分数排行。
4. 导出当前排行榜时，也按当前严格度筛选结果导出。

三、本次排行榜排版调整
1. 候选人信息、分数、证据置信度、岗位/严格度、模型/令牌分列显示。
2. 分数使用深色胶囊样式突出。
3. 证据置信度使用浅色标签显示。
4. 岗位和严格度在同一列中上下分层，比例更协调。

四、自检结果
- 核心文件完整：通过；[]
- JS 语法检查：通过；main.js:通过 ；src/main/main.js:通过 ；src/main/preload.js:通过 ；src/renderer/renderer.js:通过 
- HTML ID 与前端调用：通过；[]
- 按钮绑定：通过；[]
- 事件处理函数引用：通过；[]
- IPC 接口对应：通过；[]
- 模型选择不为空与默认模型：通过；HTML 静态选项 6 个；fallback 存在；默认 deepseek-reasoner 是
- 严格度隔离排行榜：通过；严格度筛选、过滤与提示已检查
- 排行榜美化样式：通过；排行榜卡片化行样式已检查
- 重置筛选函数：通过；已定义
- PDF/Word/TXT 读取逻辑：通过；safeBaseName/pdfParse/mammoth 已检查
- 历史误替换残留：通过；{'path.常驻name': 0, 'get深度求索Models': 0, 'export排行榜表格': 0, 'total令牌s': 0, 'prompt令牌s': 0, '兼容兼容': 0}
- 版本号：通过；1.0.20

五、建议人工测试
1. 打开软件后检查模型选择下拉框是否有选项。
2. 点击保存模型选择，确认没有报错。
3. 点击排行榜重置筛选，确认不会无反应。
4. 用 1度、3度、5度分别评分同一候选人并加入排行榜。
5. 在排行榜中切换严格度，确认不同严格度的分数不会混排。
6. 检查排行榜排版是否整齐。