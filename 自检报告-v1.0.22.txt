简历岗位匹配评分系统 v1.0.22 自检报告

一、本次调整
1. 排行榜不再显示每个候选人的令牌量。
2. 排行榜表头由“模型与令牌”改为“模型”。
3. 结果页仍保留总令牌、预提取令牌、深度评分令牌。
4. CSV导出与备份仍保留Token字段，便于成本统计。

二、自检结果
- JS语法检查：通过；main.js:通过 ；src/main/main.js:通过 ；src/main/preload.js:通过 ；src/renderer/renderer.js:通过 
- HTML ID 与前端调用：通过；[]
- 按钮绑定：通过；[]
- 排行榜不显示单人令牌量：通过；表头已改为模型：True；行内token展示已删除：True
- 结果页Token汇总保留：通过；总/预提取/深度评分Token仍保留
- AI预提取：通过；存在
- 最多9张图片限制：通过；存在
- 严格度隔离排行榜：通过；存在
- 模型选择兜底：通过；存在
- PDF/Word/TXT读取：通过；存在
- 历史误替换残留：通过；{'path.常驻name': 0, 'get深度求索Models': 0, 'export排行榜表格': 0, 'total令牌s': 0, 'prompt令牌s': 0, '兼容兼容': 0}
- 版本号：通过；1.0.22

三、建议人工测试
1. 完成一次候选人评分并进入排行榜。
2. 查看排行榜每一行，确认只显示模型，不显示总令牌/预提取令牌/评分令牌。
3. 打开评估结果页，确认Token汇总仍然显示。